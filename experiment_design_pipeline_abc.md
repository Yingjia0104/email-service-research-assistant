# A/B/C 实验设计：文本基线、正式图片链路、原始多模态直塞的价值对比

## 1. 实验背景

当前仓库里已经有一版图片价值实验，核心做法是：

- A 组：只给文本
- B 组：文本 + 原始图片直接塞给同一个主模型

这版实验可以粗看“原始图片输入”是否可能带来增量，但它有两个明显局限：

- 它没有经过当前项目的正式图片处理链路
- 它无法回答“我们现在这条图片 pipeline 本身有没有价值”

因此，这次实验升级为 3 组并行对比：

- A 组：纯文本基线
- B 组：当前正式图片处理链路
- C 组：文本 + 原始图片直接送入主模型

这样实验既能保留旧实验里“原图直塞”的参考价值，也能回答当前正式图片链路是否真的优于简单粗暴的多模态输入。

## 2. 实验目标

本实验希望回答 4 个问题：

1. 当前正式图片链路是否会让最终报告出现可识别的信息增量。
2. 原始图片直接送入主模型，是否也会带来可识别的信息增量。
3. 当前正式图片链路与“原图直塞主模型”相比，谁的信息质量更高、可控性更强。
4. 这些增量更像是：
   - 新增关键事实
   - 提高细节颗粒度
   - 增强结论置信度
   - 还是只是换了一种表述方式
5. 在**不设置任何图片预算和 cutoff** 的情况下，这三条链路各自的理论价值上限和时间成本分别是什么。

## 3. 实验原则

本次实验采用以下固定原则：

- 不触碰任何图片预算上限
- 不触碰任何 deep analysis cutoff
- 不让 daily/supplement 时间窗口干扰实验
- 只控制“图片如何进入模型”这一项变量
- 最终报告生成逻辑保持一致

基于当前主线最新实现，还需要额外固定两条技术口径：

- `max_deep_analysis_images: 0`
  - 等价于“不限制 deep analysis 数量”
- `max_images`
  - 不能简单通过配置 `0` 来放开
  - 如果实验里要真正取消图片总量上限，需要在实验脚本里显式 override `max_multimodal_images=None`

换句话说，这次实验测的是：

**图片链路的理论信息上限**

而不是：

- 高峰期吞吐能力
- quota 策略是否合理
- cutoff 是否影响结果

## 4. 实验样本

初版沿用当前实验 worktree 中已有样本：

- 样本目录：[sample-emails-0313](/Users/yyukichen/.codex/worktrees/7e19/email-service-research-assistant/sample-emails-0313)
- 当前样本数：4 封 `.msg`

这样做的好处是：

- 能和现有实验文档直接对照
- 样本里已经包含较多图片附件
- 可以复用之前的实验报告格式

## 5. 对照组与实验组

### A 组：文本基线组

定义：

- 禁用图片处理链路
- 不做 prescreen
- 不做 lightweight classification
- 不做 deep analysis
- 不做 visual context backfill
- 最终只基于邮件正文 + 可提取附件文本生成报告

目的：

- 提供一个“没有任何图片增量”的纯文本基线

### B 组：正式图片链路组

定义：

- 启用当前正式图片处理链路
- 收图阶段走 `collect_multimodal_images`
- 执行 prescreen
- 使用 `visual_llm_fast` 执行 lightweight classification
- 使用 `visual_llm` 执行 deep analysis
- 执行 visual context backfill
- 主摘要阶段消费邮件级 visual context，而不是重新直传原始图片
- 最终报告仍走同一套报告生成逻辑

目的：

- 测量“当前正式图片处理链路”相对于纯文本基线的实际增量
- 测量这条结构化链路相对于原图直塞方案的产品化价值

基于当前主线最新实现，B 组还应明确包含以下口径：

- `classification_concurrency` 与 `deep_analysis_concurrency` 由 `multimodal` 配置控制
- 邮件级视觉状态以 `ready / empty` 为主口径，历史 `partial` 会被归并
- 一旦邮件已有 `_visual_status`，主摘要阶段优先消费 visual context，不再重复直传原图

### C 组：原始多模态直塞组

定义：

- 不走当前正式图片处理链路
- 不做 prescreen
- 不做 lightweight classification
- 不做 deep analysis
- 不做 visual context backfill
- 直接将正文文本与原始图片一起送入主模型
- 最终由主模型直接输出报告

目的：

- 测量“原始图片直塞主模型”相对于纯文本基线的实际增量
- 对照 B 组，判断正式图片链路是否优于最简单的多模态方案

## 6. 三组的唯一变量

三组必须保持下面这些条件一致：

- 同一批邮件样本
- 同一份最终报告生成 prompt / schema / 输出格式
- 同一套 merge 逻辑
- 同一主摘要模型
- 同一运行环境

唯一允许变化的变量是：

- 图片是否被完全禁用
- 图片是否先经过正式图片处理链路
- 图片是否以原始形式直接进入主模型

## 7. 明确不做的事

为了避免实验变量被污染，这次明确不做下面这些事：

- 不测 quota、max_images、max_deep_analysis_images
- 不测 stop_new_deep_analysis_before_daily_minutes
- 不测高峰期降级策略
- 不引入“图片深分析联读正文”的新方案

这些都属于后续优化议题，不属于本次 A/B/C 的核心变量。

## 8. 具体执行方式

### A 组执行

步骤：

1. 解析 `.msg` 邮件样本
2. 提取正文、HTML、可读附件文本
3. 显式跳过图片链路
4. 直接进入最终报告生成
5. 保存 A 组报告

输出：

- 一份 A 组报告
- 一份运行摘要
  - 邮件数
  - 图片总数
  - 实际跳过图片数

### B 组执行

步骤：

1. 解析 `.msg` 邮件样本
2. 收集所有图片
3. 执行 prescreen
4. 使用 `visual_llm_fast` 执行 lightweight classification
5. 使用 `visual_llm` 执行 deep analysis
6. 将视觉结论回填成 visual context
7. 基于 enriched text 进入最终报告生成
8. 保存 B 组报告

输出：

- 一份 B 组报告
- 一份运行摘要
  - 邮件数
  - 图片总数
  - prescreen 通过数
  - lightweight classification 处理数
  - deep analysis 处理数
  - 生成 visual context 的邮件数
  - 轻分类实际模型名称
  - 深分析实际模型名称
  - 并发配置
  - `visual_status` 分布

实验执行时，B 组还需要额外保证：

- 不使用图片预算上限截断样本
- 不使用 deep analysis cutoff
- 不因 daily/supplement 时间窗口而跳过图片链路
- 不把原始图片再直传给最终主摘要模型

### C 组执行

步骤：

1. 解析 `.msg` 邮件样本
2. 提取正文、HTML、可读附件文本
3. 收集所有原始图片
4. 不经过正式图片链路，直接将文本与图片一起送入主模型
5. 由主模型直接生成最终报告
6. 保存 C 组报告

输出：

- 一份 C 组报告
- 一份运行摘要
  - 邮件数
  - 图片总数
  - 实际送入主模型的图片数
  - 主模型名称
  - 主模型调用次数
  - 是否发生因上下文或多模态失败导致的降级重试

## 9. 结果对比维度

实验完成后，重点比较以下几类差异：

### 9.1 信息增量

- B 组是否新增了 A 组没有提到的关键事实
- C 组是否新增了 A 组没有提到的关键事实
- B 组与 C 组谁新增了更多关键 ticker、时间点、数字、结构关系

### 9.2 细节颗粒度

- B 组是否把 A 组里的笼统说法变成了更具体的判断
- C 组是否也能做到类似增强
- B 组与 C 组谁能给出更明确、更稳定的图片证据支撑

### 9.3 结构影响

- B 组是否改变了报告重点排序
- C 组是否改变了报告重点排序
- B 组与 C 组是否改变了 actionable ideas 的强弱程度

### 9.4 真实有效性

- B 组的差异是否来自正式图片链路本身
- C 组的差异是否来自原始多模态输入本身
- 两者是否只是文风变化、表述压缩、模型随机性

## 10. 预期可能出现的结论

本实验预期可能出现 4 类结果：

### 结果 A：存在明显信息增量

表现：

- B 组新增多个 A 组没有的信息点
- C 组也可能新增多个 A 组没有的信息点
- 这些信息点能显著影响结论、标的映射或交易想法

可能结论：

- 图片总体具备明确价值
- 后续重点转向比较 B 组与 C 组谁更值得产品化

### 结果 B：存在中等增量，但更偏细节强化

表现：

- B 组没有大量新增事实
- C 组也可能没有大量新增事实
- 但其中一组在细节颗粒度、证据支撑、逻辑结构上更完整

可能结论：

- 图片链路有价值，但价值主要体现在“提高报告质量”，不是“创造大量新信息”
- 后续需要进一步优化筛选、深分析命中率，或比较正式链路与原图直塞的稳定性

### 结果 C：增量很弱

表现：

- B 组与 A 组高度相似
- C 组与 A 组也高度相似
- 差异主要体现在表述方式，而非事实或观点层面

可能结论：

- 图片整体边际价值有限
- 后续要重新审视：
  - 图片筛选是否有效
  - 深分析 prompt 是否有信息压缩问题
  - visual context 回填是否真正进入最终报告主链

### 结果 D：C 组强于 B 组，或 B 组强于 C 组

表现：

- `B vs C` 出现明显分化
- 其中一组在新增信息、结构稳定性、证据可追溯性上显著更优

可能结论：

- 如果 C 组强于 B 组：
  - 说明当前正式图片链路可能压缩掉了部分原始视觉信息
  - 后续应重点审视 prescreen、classification、deep analysis 和 backfill 的信息损耗
- 如果 B 组强于 C 组：
  - 说明正式图片链路具备产品化价值
  - 后续应重点优化吞吐、cutoff 和优先级，而不是回退到“原图直塞”

## 10.5 增量信息归因框架

如果实验结果显示 B 组或 C 组存在增量信息，下一步不能只停在“图片有用”，还要继续回答：

**这些增量到底是由哪些图片、哪些图片类型带来的。**

这是因为本实验的最终目标不是证明图片有无价值，而是为后续的：

- cutoff 设计
- deep analysis 名额分配
- 图片优先级排序

提供依据。

### 归因层级一：邮件级归因

先判断：

- 哪些邮件在 B 组相对于 A 组出现明显新增信息
- 哪些邮件在 C 组相对于 A 组出现明显新增信息
- 哪些邮件即使启用图片能力也几乎没有变化

这一步的意义是：

- 看图片价值是不是集中在少数邮件
- 判断后续是否需要邮件级优先级

### 归因层级二：图片类型级归因

对每一条 B 组新增的信息点，标记它最可能来自哪类图片：

- `market_data_chart`
- `social_signal_visual`
- `research_framework_chart`
- `editorial_framing_visual`

这一步回答的是：

- 哪类图片最常带来信息增量
- 哪类图片更容易带来“可交易”的增量
- 哪类图片大多只是弱增强或装饰性补充

### 归因层级三：单图级归因

在实验运行过程中，为 B 组的每条视觉结论保留最基本的来源追踪：

- `email_id`
- `image_key`
- `image_type`
- `role_in_email`
- `analysis_stage`

这样在做 A/B/C 对比时，可以把 B 组新增的信息回链到：

- 是哪一封邮件
- 是哪一张图
- 属于什么图片类型
- 来自 lightweight classification 还是 deep analysis

### 建议记录的归因指标

为了让实验结果直接服务于后续的 quota 和优先级设计，建议输出至少这几类统计：

- `incremental_facts_by_image_type`
  - 每类图片带来了多少条新增事实
- `decision_relevant_facts_by_image_type`
  - 每类图片带来了多少条会影响 ticker、催化剂、风险或交易想法的新增信息
- `low_value_hits_by_image_type`
  - 每类图片被分析后，最终却没有形成有效增量的次数
- `marginal_value_per_image_type`
  - 每类图片的单位处理成本，对应换来了多少有效增量

### 归因结果如何反哺 cutoff 和优先级

实验完成后，希望能倒推出类似下面的判断：

- 如果 `social_signal_visual` 经常带来新的、可交易的信息
  - 那它在高峰期应保持最高优先级
- 如果 `market_data_chart` 也经常带来强增量
  - 那它应位于第二优先级
- 如果 `editorial_framing_visual` 大多只是强化语气、很少新增可交易信息
  - 那它就是最适合被提前 cutoff 的类型
- 如果某类图片的 deep analysis 成本明显高于其边际价值
  - 那这类图片应该被降权，或者只在低负载时处理

同时还希望回答：

- C 组是否虽然更粗暴，但信息捕捉更充分
- B 组是否虽然更复杂，但在可控性和可归因性上更强

换句话说，本实验最终不是只回答：

**“图片有没有用？”**

而是要尽量逼近：

**“哪类图片最值得抢有限的图片处理资源，以及正式链路是否比原图直塞更值得保留？”**

## 11. 这次实验最重要的验收标准

我建议这次先不把“报告长短不同”当核心结论，而是看下面三件事：

1. B 组是否出现了 A 组没有的关键信息
2. 这些信息是否能追溯到图片链路输出
3. 这些增量是否足以影响研究判断或交易映射

只有这三点成立，才能说：

**当前图片处理链路对最终报告确实有实质贡献。**

## 12. 耗时评估框架

除了信息增量之外，这次实验还必须同步评估 3 条链路的耗时。

原因很直接：

- 最终我们需要决定 cutoff
- 也需要决定哪些图片类型值得优先处理
- 因此不能只看“有没有增量”，还要看“这些增量值不值得花这么多时间”

### 12.1 必须保留的两类耗时指标

本次实验中，下面两类耗时都要保留：

#### 1. 总耗时

定义：

- 每组从实验开始到最终报告产出的完整 wall-clock time

意义：

- 反映真实用户体验
- 反映盘前时段这条链路是否可落地
- 回答“这条链路到底要多久”

#### 2. 增量成本耗时

定义：

- `B 组总耗时 - A 组总耗时`
- `C 组总耗时 - A 组总耗时`

意义：

- 反映为了引入图片能力，额外多花了多少时间
- 回答“这些额外时间到底换来了多少价值”

### 12.2 建议记录的原始耗时数据

每组建议至少记录以下字段：

- `total_runtime_seconds`
- `non_llm_runtime_seconds`
- `llm_runtime_seconds`
- `num_llm_calls`
- `avg_llm_call_seconds`
- `median_llm_call_seconds`

其中：

- `total_runtime_seconds`
  - 用来看真实端到端耗时
- `non_llm_runtime_seconds`
  - 用来看本地解析、图片整理、回填、组装等非模型开销
- `llm_runtime_seconds`
  - 用来看模型调用在总耗时中占比

### 12.3 如何尽量降低服务端波动噪音

模型服务端的实时波动很难完全消除，所以实验不追求“扣干净服务端耗时”，而是通过方法设计尽量降低噪音：

- 每组至少重复运行多轮
- 采用轮转顺序，而不是固定顺序
- 最终优先看中位数，而不是单次结果

推荐做法：

- 第 1 轮：A -> B -> C
- 第 2 轮：B -> C -> A
- 第 3 轮：C -> A -> B

最后汇总：

- `median_total_runtime_seconds`
- `median_llm_runtime_seconds`
- `median_num_llm_calls`

### 12.4 与增量信息结合后的成本指标

为了让实验结果直接服务于 cutoff 和优先级设计，建议在耗时之外再计算下面这些派生指标：

- `seconds_per_incremental_fact`
  - 每多得到 1 条新增有效信息，需要多花多少秒
- `seconds_per_decision_relevant_fact`
  - 每多得到 1 条会影响 ticker、催化剂、风险或交易想法的信息，需要多花多少秒

这两个指标的意义非常大：

- 如果一条链路总耗时更长，但单位增量成本更低
  - 它仍然可能是值得的
- 如果一条链路虽然有增量，但单位增量成本极高
  - 那它更适合被 cutoff 或降权

### 12.5 三组耗时结论希望回答什么

#### A 组

- 给出纯文本基线耗时
- 作为所有增量耗时的参考底座

#### B 组

- 评估当前正式图片链路的完整成本
- 判断这条链路的增量是否值得其处理复杂度

#### C 组

- 评估“原图直塞主模型”的时间成本
- 与 B 组比较“结构化图片链路”和“粗暴多模态直塞”谁更划算

### 12.6 最终希望形成的耗时结论

实验完成后，希望耗时结论至少能支持下面这些判断：

- 哪一组总耗时最低
- 哪一组总耗时最高
- 哪一组相对 A 组的增量耗时最小
- 哪一组单位新增有效信息的耗时最低
- 哪一组单位新增决策相关信息的耗时最低
- B 组与 C 组相比，谁的总耗时更可接受
- B 组与 C 组相比，谁的单位增量成本更低

最终我们不是只看：

- “哪组更快”

而是同时看：

- “哪组更值”

## 13. 预期产物

本次实验完成后，预期生成这些文档：

- `experiment_report_a_text_only.md`
- `experiment_report_b_pipeline_with_images.md`
- `experiment_report_c_raw_images.md`
- `experiment_comparison_abc.md`
- `experiment_runtime_summary_abc.json`

## 14. 下一步执行建议

如果这份设计确认无误，下一步就按下面顺序执行：

1. 改实验脚本，让它支持 A/B/C 三组
2. 明确 A 组如何彻底禁用图片链路
3. 明确 B 组如何放开所有图片预算和 cutoff，并走当前正式 pipeline
4. 明确 C 组如何把文本和原始图片直接送入主模型
5. 运行实验
6. 输出 A/B/C 报告、对比文档和耗时摘要
